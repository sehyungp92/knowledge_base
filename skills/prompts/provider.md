# Provider Routing

This command is handled directly by the Python gateway.

- `/provider` shows the current chat provider and which options are available.
- `/provider claude`, `/provider codex`, `/provider zai`, and `/provider openrouter` update the sticky provider for the current chat.

Do not call external tools for this command.
